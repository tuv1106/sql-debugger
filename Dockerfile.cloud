FROM python:3.14-slim

WORKDIR /app

COPY cloud_backend/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY cloud_backend/ ./cloud_backend/

EXPOSE 8000

CMD ["uvicorn", "cloud_backend.main:app", "--host", "0.0.0.0", "--port", "8000"]
